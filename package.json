{
  "name": "jchip8",
  "version": "1.0.0",
  "description": "chip8 emulator",
  "main": "src/main.js",
  "scripts": {
    "test": "jest tests",
    "bundle": "browserify src/main.js -o dist/main.js",
    "serve": "serve dist",
    "develop": "nodemon --ignore dist/ --exec 'npm run bundle && npm run serve'"
  },
  "author": "Willem Clarke",
  "license": "ISC",
  "dependencies": {
    "lodash": "^4.17.13"
  },
  "devDependencies": {
    "serve": "^11.0.2",
    "browserify": "^16.2.3",
    "jest": "^24.8.0",
    "nodemon": "^1.19.1",
    "prettier": "^1.17.0"
  }
}
